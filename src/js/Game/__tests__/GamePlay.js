/**
 * К сожалению мне не понянто, что и каким образом
 * здесь можно протестировать с помощью jest
 * поэтому тест пока в качестве залушки,
 * чтобы appveyor не ругался
 */

test('test nothin', () => {
  expect(1).toBe(1);
});
